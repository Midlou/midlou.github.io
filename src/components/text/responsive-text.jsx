import React from "react";

const ResponsiveText = ({ children }) => {
	return <p className="text-white mb-6 mt-3 text-base sm:text-lg md:mt-5 md:text-xl md:max-w-3xl">{children}</p>;
}

export default ResponsiveText;
